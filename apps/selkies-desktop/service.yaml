apiVersion: v1
kind: Service
metadata:
  name: selkies-desktop
  namespace: cloud-desktop
  labels:
    app.kubernetes.io/name: selkies-desktop
spec:
  selector:
    app.kubernetes.io/name: selkies-desktop
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
