apiVersion: v1
kind: Service
metadata:
  name: immich-redis
  namespace: immich
spec:
  selector:
    app.kubernetes.io/name: immich-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: immich-redis
  namespace: immich
spec:
  serviceName: immich-redis
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: immich-redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: immich-redis
    spec:
      containers:
        - name: valkey
          image: valkey/valkey:8.1.10-alpine
          command:
            - /bin/sh
            - -ec
            - exec valkey-server --save "" --appendonly no --requirepass "$REDIS_PASSWORD"
          ports:
            - name: redis
              containerPort: 6379
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ${immich_secret_name}
                  key: REDIS_PASSWORD
          resources:
            requests:
              cpu: ${immich_redis_requests_cpu}
              memory: ${immich_redis_requests_memory}
            limits:
              cpu: ${immich_redis_limits_cpu}
              memory: ${immich_redis_limits_memory}
          readinessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 6379
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: ${immich_redis_storage_class_name}
        resources:
          requests:
            storage: ${immich_redis_size}
