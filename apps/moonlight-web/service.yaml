apiVersion: v1
kind: Service
metadata:
  name: moonlight-web
  namespace: media
  labels:
    app.kubernetes.io/name: moonlight-web
spec:
  selector:
    app.kubernetes.io/name: moonlight-web
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

