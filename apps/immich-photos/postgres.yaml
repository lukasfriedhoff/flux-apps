apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-photos-postgres
  namespace: immich-photos
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:18.4-system-trixie
  instances: ${immich_photos_postgres_instances:=1}
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${immich_photos_postgres_source_dbname:=immich}
      owner: ${immich_photos_postgres_source_user:=immich}
      secret:
        name: immich-photos-postgres-app
      postInitApplicationSQL:
        - CREATE EXTENSION IF NOT EXISTS vector;
  managed:
    roles:
      - name: ${immich_photos_postgres_source_user:=immich}
        ensure: present
        login: true
        superuser: true
        passwordSecret:
          name: immich-photos-postgres-app
  storage:
    storageClass: ${immich_photos_postgres_storage_class_name}
    size: ${immich_photos_postgres_size}
  # BACKUPS DISABLED 2026-09-22 (storage01 MinIO at 100%) - see the
  # nextcloud postgres.yaml for the full incident note. Revert to re-enable.
  resources:
    requests:
      cpu: ${immich_photos_postgres_requests_cpu}
      memory: ${immich_photos_postgres_requests_memory}
    limits:
      cpu: ${immich_photos_postgres_limits_cpu}
      memory: ${immich_photos_postgres_limits_memory}
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: immich-photos-postgres
  namespace: immich-photos
spec:
  suspend: true
  schedule: "${immich_photos_postgres_backup_cron:=0 0 2 * * *}"
  backupOwnerReference: self
  immediate: false
  cluster:
    name: immich-photos-postgres
