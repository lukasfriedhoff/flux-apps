apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: authelia-postgres
  namespace: authelia
spec:
  suspend: true
  schedule: "${authelia_postgres_backup_cron:=0 0 2 * * *}"
  backupOwnerReference: self
  immediate: false
  cluster:
    name: authelia-postgres
