apiVersion: v1
kind: Service
metadata:
  name: synapsedb-source
  namespace: matrix
  annotations:
    lukasf.io/migration-temporary: "true"
spec:
  selector:
    app.kubernetes.io/name: synapsedb
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: synapsedb
  namespace: matrix
spec:
  # Keep the historical service name stable for app-side config.
  selector:
    cnpg.io/cluster: synapsedb
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: synapsedb
  namespace: matrix
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16-standard-trixie
  instances: 1
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${matrix_synapse_postgres_source_dbname:=synapse}
      owner: ${matrix_synapse_postgres_source_user:=synapse}
      import:
        type: microservice
        databases:
          - ${matrix_synapse_postgres_source_dbname:=synapse}
        source:
          externalCluster: synapsedb-source
  externalClusters:
    - name: synapsedb-source
      connectionParameters:
        host: synapsedb-source
        user: ${matrix_synapse_postgres_source_user:=synapse}
        dbname: ${matrix_synapse_postgres_source_dbname:=synapse}
      password:
        name: matrix-db
        key: SYNAPSE_DB_PASSWORD
  storage:
    storageClass: ${matrix_postgres_storage_class_name}
    size: ${matrix_postgres_size}
  resources:
    requests:
      cpu: ${matrix_postgres_requests_cpu}
      memory: ${matrix_postgres_requests_memory}
    limits:
      cpu: ${matrix_postgres_limits_cpu}
      memory: ${matrix_postgres_limits_memory}
---
apiVersion: v1
kind: Service
metadata:
  name: whatsappdb-source
  namespace: matrix
  annotations:
    lukasf.io/migration-temporary: "true"
spec:
  selector:
    app.kubernetes.io/name: whatsappdb
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: whatsappdb
  namespace: matrix
spec:
  selector:
    cnpg.io/cluster: whatsappdb
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: whatsappdb
  namespace: matrix
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16-standard-trixie
  instances: 1
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${matrix_whatsapp_postgres_source_dbname:=mautrixwhatsapp}
      owner: ${matrix_whatsapp_postgres_source_user:=mautrixwhatsapp}
      import:
        type: microservice
        databases:
          - ${matrix_whatsapp_postgres_source_dbname:=mautrixwhatsapp}
        source:
          externalCluster: whatsappdb-source
  externalClusters:
    - name: whatsappdb-source
      connectionParameters:
        host: whatsappdb-source
        user: ${matrix_whatsapp_postgres_source_user:=mautrixwhatsapp}
        dbname: ${matrix_whatsapp_postgres_source_dbname:=mautrixwhatsapp}
      password:
        name: matrix-db
        key: WHATSAPP_DB_PASSWORD
  storage:
    storageClass: ${matrix_postgres_storage_class_name}
    size: ${matrix_postgres_size}
  resources:
    requests:
      cpu: ${matrix_postgres_requests_cpu}
      memory: ${matrix_postgres_requests_memory}
    limits:
      cpu: ${matrix_postgres_limits_cpu}
      memory: ${matrix_postgres_limits_memory}
---
apiVersion: v1
kind: Service
metadata:
  name: wadb-source
  namespace: matrix
  annotations:
    lukasf.io/migration-temporary: "true"
spec:
  selector:
    app.kubernetes.io/name: wadb
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: wadb
  namespace: matrix
spec:
  selector:
    cnpg.io/cluster: wadb
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: wadb
  namespace: matrix
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16-standard-trixie
  instances: 1
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${matrix_wa_postgres_source_dbname:=mautrixwhatsapp}
      owner: ${matrix_wa_postgres_source_user:=mautrixwhatsapp}
      import:
        type: microservice
        databases:
          - ${matrix_wa_postgres_source_dbname:=mautrixwhatsapp}
        source:
          externalCluster: wadb-source
  externalClusters:
    - name: wadb-source
      connectionParameters:
        host: wadb-source
        user: ${matrix_wa_postgres_source_user:=mautrixwhatsapp}
        dbname: ${matrix_wa_postgres_source_dbname:=mautrixwhatsapp}
      password:
        name: matrix-db
        key: WA_DB_PASSWORD
  storage:
    storageClass: ${matrix_postgres_storage_class_name}
    size: ${matrix_postgres_size}
  resources:
    requests:
      cpu: ${matrix_postgres_requests_cpu}
      memory: ${matrix_postgres_requests_memory}
    limits:
      cpu: ${matrix_postgres_limits_cpu}
      memory: ${matrix_postgres_limits_memory}
---
apiVersion: v1
kind: Service
metadata:
  name: telegramdb-source
  namespace: matrix
  annotations:
    lukasf.io/migration-temporary: "true"
spec:
  selector:
    app.kubernetes.io/name: telegramdb
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: telegramdb
  namespace: matrix
spec:
  selector:
    cnpg.io/cluster: telegramdb
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: telegramdb
  namespace: matrix
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16-standard-trixie
  instances: 1
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${matrix_telegram_postgres_source_dbname:=mautrixtelegram}
      owner: ${matrix_telegram_postgres_source_user:=mautrixtelegram}
      import:
        type: microservice
        databases:
          - ${matrix_telegram_postgres_source_dbname:=mautrixtelegram}
        source:
          externalCluster: telegramdb-source
  externalClusters:
    - name: telegramdb-source
      connectionParameters:
        host: telegramdb-source
        user: ${matrix_telegram_postgres_source_user:=mautrixtelegram}
        dbname: ${matrix_telegram_postgres_source_dbname:=mautrixtelegram}
      password:
        name: matrix-db
        key: TELEGRAM_DB_PASSWORD
  storage:
    storageClass: ${matrix_postgres_storage_class_name}
    size: ${matrix_postgres_size}
  resources:
    requests:
      cpu: ${matrix_postgres_requests_cpu}
      memory: ${matrix_postgres_requests_memory}
    limits:
      cpu: ${matrix_postgres_limits_cpu}
      memory: ${matrix_postgres_limits_memory}
---
apiVersion: v1
kind: Service
metadata:
  name: signaldbprivate-source
  namespace: matrix
  annotations:
    lukasf.io/migration-temporary: "true"
spec:
  selector:
    app.kubernetes.io/name: signaldbprivate
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: signaldbprivate
  namespace: matrix
spec:
  selector:
    cnpg.io/cluster: signaldbprivate
    cnpg.io/instanceRole: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: signaldbprivate
  namespace: matrix
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:16-standard-trixie
  instances: 1
  primaryUpdateStrategy: unsupervised
  inheritedMetadata:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9187"
      prometheus.io/path: "/metrics"
  bootstrap:
    initdb:
      database: ${matrix_signal_postgres_source_dbname:=mautrixsignal}
      owner: ${matrix_signal_postgres_source_user:=mautrixsignal}
      import:
        type: microservice
        databases:
          - ${matrix_signal_postgres_source_dbname:=mautrixsignal}
        source:
          externalCluster: signaldbprivate-source
  externalClusters:
    - name: signaldbprivate-source
      connectionParameters:
        host: signaldbprivate-source
        user: ${matrix_signal_postgres_source_user:=mautrixsignal}
        dbname: ${matrix_signal_postgres_source_dbname:=mautrixsignal}
      password:
        name: matrix-db
        key: SIGNAL_DB_PASSWORD
  storage:
    storageClass: ${matrix_postgres_storage_class_name}
    size: ${matrix_postgres_size}
  resources:
    requests:
      cpu: ${matrix_postgres_requests_cpu}
      memory: ${matrix_postgres_requests_memory}
    limits:
      cpu: ${matrix_postgres_limits_cpu}
      memory: ${matrix_postgres_limits_memory}
